<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="BaseAppCardView" parent="CardView">
        <item name="cardPreventCornerOverlap">false</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <style name="TextAppearance.AppCompat.Body1.Disable" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/textDisabled</item> <!-- body1 is #000000 -->
    </style>

    <style name="TextAppearance.AppCompat.Small.Disable" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/textDisabled</item>
    </style>

    <style name="NoCommentsTextAppearance" parent="TextAppearance.AppCompat.Large">
        <item name="android:textColor">@color/colorDivider</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
